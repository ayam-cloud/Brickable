# Database (SQLite file for local/dev; swap for a hosted Postgres URL in production)
DATABASE_URL="file:./dev.db"

# Stripe (https://dashboard.stripe.com/test/apikeys)
STRIPE_SECRET_KEY="sk_test_..."
STRIPE_WEBHOOK_SECRET="whsec_..."

# Used to build absolute redirect URLs for Stripe Checkout. Set to your deployed URL in production.
NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# Admin dashboard (/admin) — pick your own values
ADMIN_PASSWORD="change-me"
ADMIN_SESSION_SECRET="change-me-too"
